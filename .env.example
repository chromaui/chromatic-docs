# Chromatic docs site — local environment.
# Copy to `.env` (gitignored) and fill in real values.
# Production/preview values live in the `chromatic2-docs` Netlify site settings,
# not GitHub Secrets.
#
# Plain and Intercom only load during `pnpm build && pnpm preview` — the
# `isProduction` gate in src/components/Scripts.astro skips them during `pnpm dev`.

PUBLIC_CHROMATIC_PLAIN_APP_ID=liveChatApp_01KHKJ4G8WRFY8W0CA6RAXRY7A
PUBLIC_CHROMATIC_INTERCOM_APP_ID=zj7sn9j1
