---
title: FAQs
description: Frequently asked questions about Chromatic
sidebar: { order: 1 }
slug: faq
---

# Frequently asked questions

If you have a question not covered in our documentation, contact us via our in-app chat.

## Comparison with other tools

<ul class="faq-section-toc">
  {props.groupedFAQs.comparisons.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## Usage

<ul class="faq-section-toc">
  {props.groupedFAQs.usage.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## Browsers

<ul class="faq-section-toc">
  {props.groupedFAQs.browsers.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## UI Tests and UI Review

<ul class="faq-section-toc">
  {props.groupedFAQs.uiTestsAndReview.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## Publish

<ul class="faq-section-toc">
  {props.groupedFAQs.publish.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## Continuous Integration

<ul class="faq-section-toc">
  {props.groupedFAQs.ci.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## TurboSnap

<ul class="faq-section-toc">
  {props.groupedFAQs.turboSnap.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## Storybook Connect Figma plugin

<ul class="faq-section-toc">
  {props.groupedFAQs.figmaPlugin.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## Figma in Chromatic

<ul class="faq-section-toc">
  {props.groupedFAQs.figmaInChromatic.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## Compatibility

<ul class="faq-section-toc">
  {props.groupedFAQs.compatibility.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## Your account

<ul class="faq-section-toc">
  {props.groupedFAQs.account.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>

## Network

<ul class="faq-section-toc">
  {props.groupedFAQs.network.map((faq) => (
    <li>
      <a href={`/docs/${faq.id}`}>{faq.title}</a>
    </li>
  ))}
</ul>