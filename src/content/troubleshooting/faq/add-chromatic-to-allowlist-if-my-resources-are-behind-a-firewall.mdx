---
sidebar: { hide: true }
title: How do I add chromatic.com to my allowlist if my resources are behind a firewall?
section: compatibility
---

# How do I add chromatic.com to my allowlist if my resources are behind a firewall?

You will need to make sure that `chromatic.com` is added to the allowed list in your firewall settings. Since `chromatic.com` is a top-level domain, you won't need to add any subdomains to the allowlist.
