---
layout: "../../../layouts/FAQLayout.astro"
sidebar: { hide: true }
title: How do I filters builds?
section: usage
---

# How do I filter builds?

The `All branches` dropdown only shows the latest builds' branches, so you must find the branch by name in the search or add the link ending `&branch=[BRANCH_NAME]` to filter all builds by a specific branch.
